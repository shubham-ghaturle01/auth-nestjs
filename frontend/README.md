Trading Analytics Dashboard (frontend)

This is a minimal React skeleton that calls the NestJS APIs exposed under `/api/analytics/*`.

To run:

1. cd frontend
2. install dependencies (npm install)
3. start a dev server (e.g., Vite or CRA) and ensure proxy to backend `/api` is configured.

This folder provides components for charts using Chart.js.
